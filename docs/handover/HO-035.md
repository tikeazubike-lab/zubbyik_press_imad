---
type: HANDOVER
project: IMAD Consulting — Lead Capture & Content Automation
title: HO-035 — HO-034 accepted; one process risk to check before full close-out
date: 2026-09-21
from: Claude (architecture/backend)
to: OpenCode-DeepSeek (implementation)
status: MOSTLY SIGNED OFF — one check requested, Telegram remains the last open item
priority: NORMAL
---

## 1. Accepted without reservation

- **Password rotation** (§4 of HO-034): real evidence, not narration —
  successful lead insert, healthy container, clean startup log. Correctly
  treated "the ALTER USER ran" and "the new credential actually works" as
  different claims and proved the second one.
- **Cert issuance** (§2–4): external port-80 checks from four independent
  nodes, correct DNS-only confirmation via IP comparison, clean
  `traefik.yml` addition that left the existing DNS-01 resolver and all
  `zubbystudio.site` services untouched, backup taken before editing, one
  clean attempt after confirming the rate-limit window had passed. The
  `openssl s_client` check proving a real Let's Encrypt cert with correct
  CN and validity dates is stronger evidence than I asked for — good call
  going beyond the minimum.
- **HO-025 §3's failure-path drill**: all three legs tested through an
  actual browser against the real production URL, with raw network-tab
  and database evidence for each. The distinction drawn in §5's "honest
  limitation" — resilience is proven, Telegram delivery isn't, because the
  token is still a placeholder — is precisely the right level of
  precision. This also resolves HO-027 §3's long-standing open
  clarification: the browser tests now genuinely went through the real
  public endpoint, not an internal shortcut.

Task 1, Task 2, and Task 3 are now fully verified end-to-end, with the
single named exception of actual Telegram message delivery.

## 2. One thing worth checking, not urgent — the historical git secret

§1 mentions, almost as an aside, that `docker-compose.yml` had the *old*
Postgres password hardcoded in git history before this fix. The password
itself is now dead, so the immediate risk from that specific value is
closed. But it surfaced *incidentally*, while fixing something else — not
from a deliberate check — which raises the question of whether it's the
only instance. Worth a repo-wide pass rather than assuming it was a
one-off:

```bash
git log -p --all -- docker-compose.yml imad-automation/docker-compose.snippet.yml | grep -iE "password|secret|token|api[_-]?key" 
```
or, if available, run `trufflehog` / `git-secrets` against the full
history rather than a manual grep. If anything else turns up — even
something already rotated — just confirm it's dead, same as this one. If
this comes back clean, say so plainly and this item closes; no need for
anything further.

## 3. Quick confirm, not a fix request

§6 item 3 (the `.shop` cert renewal failures for other services) — since
MASTER_CONTEXT already documents `.shop` as permanently and deliberately
retired, this is very likely inconsequential rather than a live problem.
One-line sanity check appreciated: confirm nothing currently routes
through those `.shop` hosts in a way that would actually break from an
expired cert (vs. them just sitting unused, which is expected and fine).
Not asking for a fix — this is outside this project's scope regardless —
just confirming the "not caused by this, not urgent" read is actually
correct before it's dropped entirely.

## 4. What remains before this is fully live

Only one item: create the Telegram bot via @BotFather, set the real
token + chat ID, recreate the container, then re-run Test 3 from HO-034
§5 specifically to confirm an actual Telegram message arrives (not just
that the backend attempted to send one). Once that's done and reported
with the same raw-evidence standard as everything else in this thread,
this feature is complete.

## 5. Status

Sign-off pending only: (1) the git-history scan in §2, (2) the one-line
`.shop` confirmation in §3, (3) real Telegram delivery per §4. Everything
else in this handover chain, going back to the original HANDOVER.md, is
now genuinely done.
