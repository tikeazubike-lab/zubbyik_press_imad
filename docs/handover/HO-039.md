---
type: HANDOVER
project: IMAD Consulting — Lead Capture & Content Automation
title: HO-039 — Dedicated leads bot configured and delivering; thread complete
date: 2026-09-21
from: OpenCode / DeepSeek-flash (implementation)
to: Claude (architecture/backend) — final close-out
status: COMPLETE — all HO-037 items closed
priority: NORMAL
---

## 1. §2 — Dedicated bot: configured and proven

Malachy created the dedicated bot via @BotFather and messaged it. Verified
and wired up.

### The bot

```
$ curl .../getMe
{ "ok": true, "result": { "id": 8703804900, "first_name": "IMAD Leads",
  "username": "imadlead_bot", ... } }
```

### Clean separation (the point of the request)

| Purpose | Env var | Bot |
|---|---|---|
| WordPress visitor chatbot notifications | `TELEGRAM_BOT_TOKEN` | `@imadconsultingBot` (shared, unchanged) |
| Lead notifications from imad-automation | `IMAD_TELEGRAM_BOT_TOKEN` | `@imadlead_bot` (dedicated, new) |

No lead can now be lost among chatbot traffic.

### chat_id

`getUpdates` after the bot was messaged:

```
chat_id: 7743436182 | type: private | name: 07070_zubbyik | text: /start
chat_id: 7743436182 | type: private | name: 07070_zubbyik | text: hello
```

(Private-chat id equals the user id, so it is the same value as the shared
bot's chat — but it is a distinct bot and a distinct conversation.)

### Proof 1 — direct API delivery via the dedicated bot

```
DELIVERED — ok: True | message_id: 3 | chat: 07070_zubbyik | bot: dedicated
```

### Proof 2 — the app's own notify() path

```
$ docker exec imad-automation python -c "... await notify.notify(...) ..."
using bot id: 8703804900
notify() completed

dead_letters BEFORE: 4
dead_letters AFTER:  4     # unchanged -> send succeeded
```

### Proof 3 — full end-to-end via the real browser

Submitted on staging with `?utm_source=status&utm_campaign=ep001`; visitor saw
success; then:

```
 id |        name        |          contact          | source_campaign
----+--------------------+---------------------------+-----------------
  7 | Dedicated Bot Test | dedicated-bot@example.com | ep001

dead_letters count: 4      # unchanged -> delivered via the dedicated bot
```

### Token hygiene holds

```
$ docker logs imad-automation | grep -c "AAGMutiOCB3kX8H46G4RXmZ0MeIlrNaprkc"
0
```

The new token appears zero times in the container log — HO-038's fix covers
this bot too.

---

## 2. §3 — Token logging (from HO-037)

Closed in HO-038; re-confirmed here against the new dedicated token (0
occurrences). The only remaining request line in the log is uvicorn's own
`POST /api/leads HTTP/1.1 200 OK`, which carries no credential.

---

## 3. §4 — Recorded items, on your timeline

- **Rotate `wordpress_pass` / `root_pass`** — internal-only, non-blocking.
  Anchor: next maintenance window.
- **Clear test rows** — `leads` ids 1–7, `dead_letters` ids 1–4. Ready to
  clear on request; worth doing before real leads arrive.
- **`MASTER_CONTEXT.md` staleness** (`.shop` domain pattern) — out of scope.

---

## 4. Thread status — complete

| Item | Status |
|---|---|
| Task 1 — backend + valid TLS + `/healthz` | ✅ HO-034 |
| Task 2 — lead capture merged into existing Contact form | ✅ HO-034 |
| Task 3 — conditional hero CTA + `source_campaign` | ✅ HO-034 |
| HO-025 §3 failure-path drill | ✅ HO-034 |
| Password rotation | ✅ HO-034 |
| Git-history secret scan | ✅ HO-036 |
| `.shop` confirmation | ✅ HO-036 |
| Telegram token log hygiene | ✅ HO-038 |
| Dedicated leads bot | ✅ this document |

Nothing outstanding. The feature is live: a campaign link →
`/r/{source}` → homepage with UTMs → swapped hero CTA → Contact form →
lead stored with campaign attribution → Telegram notification to a
dedicated `@imadlead_bot` chat, with the email channel running alongside.
