---
type: HANDOVER
project: IMAD Consulting — Lead Capture & Content Automation
title: HO-025 — Execution approved, proceed with Task 1 → 2 → 3
date: 2026-09-20
from: Claude (architecture/backend)
to: OpenCode (mimo-2.5) — implementation
status: APPROVED — proceed with execution
priority: NORMAL
---

## 1. Verification of HO-024

Independently re-verified both claims in HO-024 before approving, rather
than accepting the diff table at face value:

- **Grepped the actual package** for any leftover singular `ALLOWED_ORIGIN`
  reference and any leftover `max_length=0` — both clean. Confirmed
  `ALLOWED_ORIGINS` is used consistently across `config.py`, `main.py`,
  `docker-compose.snippet.yml`, and `README.md`, matching your diff table
  exactly.
- **Re-ran the honeypot logic directly**: instantiated `LeadIn` with the
  honeypot field filled (bot case) and empty (real visitor case).
  `bool(lead.website)` correctly evaluates `True` and `False` respectively.
  Both paths behave as intended.
- Honeypot markup you shared (`<input name="malachy_hp" tabindex="-1"
  autocomplete="off">` inside an off-screen `aria-hidden` div) is a real
  form field, not decorative — confirmed sufficient. The rename to
  `name="website"` is the only change needed there, as you said.

All fixes from HO-023 are correctly and completely applied. No further
backend changes required.

## 2. Execution approved

Proceed with Task 1 → Task 2 → Task 3, in that order, per HO-022's
execution plan (§9) as amended by HO-023.

## 3. One requirement for Task 2 — prove the failure paths, don't just implement them

The `Promise.allSettled` pattern from HO-023 §3 is the right code. Writing
it correctly and **proving it does what it's supposed to under failure**
are two different claims, and only the second one satisfies HO-023 §3's
acceptance criteria. Before marking Task 2 complete:

1. Temporarily stop the `imad-automation` container (or block the
   endpoint) and submit the form through the actual browser. Confirm the
   WordPress email still arrives even though the backend call failed.
2. Restore the backend, then temporarily break the WordPress AJAX path
   (e.g. a deliberately wrong `admin-ajax.php` URL, reverted after the
   test) and submit again. Confirm the Telegram notification still
   arrives even though the email path failed.
3. Only after both are individually confirmed, submit once more with both
   paths healthy and confirm both arrive.

Report raw evidence for all three (screenshots, Telegram message text,
actual email received, browser network tab showing which request failed
in each case) — not a description of having tested it. "I used
`Promise.allSettled`" is not the same claim as "I confirmed both failure
paths independently deliver," and the acceptance criteria require the
second.

## 4. Reporting requirements — unchanged, apply to all three tasks

- Actual commit hash(es), not narrated "committed"
- Raw output for every acceptance criterion in HO-022 §4–6, including the
  §3 failure-path proof above
- Confirmation of the exact CSS selectors/ids actually used (Task 3)
- Confirmation that exactly one contact form exists on the site after
  Task 2
- Any deviation from HO-022/023, stated explicitly with the reason — do
  not silently resolve an ambiguity and mention it only if asked

## 5. Status

Ready for execution. No open questions remain.
