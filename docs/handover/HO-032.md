---
type: HANDOVER
project: IMAD Consulting — Lead Capture & Content Automation
title: HO-032 — Cloudflare account-boundary question for Malachy + pre-staged fix for either answer
date: 2026-09-20
from: Claude (architecture/backend)
to: Malachy (decision) / OpenCode-DeepSeek (execute once answered)
status: BLOCKED on one human check — everything else pre-staged
priority: NORMAL (§3 password rotation: URGENT, do this now regardless of the block)
---

## 1. §1–§2 of HO-031 — accepted, no further action needed

Both confirmed with raw evidence, not narration:
- HO-029's reversion is genuinely fully deployed (all three greps pass).
- The account-boundary question cannot be resolved from the API alone —
  correctly identified and escalated rather than guessed at. A zone-scoped
  token returning an empty `/accounts` list is the right read: it proves
  the token can't see across accounts, but not which side of that boundary
  `imadconsulting.co.uk` sits on.

## 2. Malachy — the one question only you can answer

Log into the Cloudflare dashboard (dash.cloudflare.com) with whichever
login manages `zubbystudio.site`. Look at the zone list on the main
account page.

- **If `imadconsulting.co.uk` appears in that same list** → same account.
  Tell OpenCode to proceed with §4a below.
- **If it doesn't appear there** → you're logging into the wrong account
  for that domain, or it's genuinely a separate account. Tell OpenCode to
  proceed with §4b below.

This takes about 30 seconds and unblocks everything else in this thread.

## 3. URGENT, unblocked by the above — rotate the Postgres password now

This has been flagged in HO-027, restated in HO-029, and restated again in
HO-031 §5 without action. It doesn't depend on the Cloudflare question at
all — there's no reason for it to still be open. `imad_user`'s password
(`imad_2026_secure`) has now appeared in plaintext across at least three
handover documents. Do this in parallel with §2, not after:

```bash
# On the VPS, against the existing shared Postgres:
docker exec -it openagile_postgres psql -U postgres -c \
  "ALTER USER imad_user WITH PASSWORD '$(openssl rand -hex 32)';"
```
Then update `IMAD_DATABASE_URL` in `.env` to match the new password,
restart `imad-automation`, and confirm `/healthz` still returns 200 (proves
the new credential actually works — don't just confirm the `ALTER USER`
ran). Report the restart + health-check output. Do not report the new
password value itself in any future handover — confirming it works is
sufficient.

## 4. Pre-staged instructions — execute whichever branch Malachy confirms

### 4a. Same account

1. Cloudflare dashboard → My Profile → API Tokens → edit the existing
   token → add `imadconsulting.co.uk` to Zone Resources → grant
   `Zone:DNS:Edit` + `Zone:Zone:Read` for that zone.
2. No `traefik.yml` change.
3. Wait for the rate-limit window to fully expire (per HO-031 §3, this was
   ≈4 minutes from that check — confirm the actual current time against
   `2026-09-20 22:08:39 UTC` before attempting anything, don't assume it's
   passed).
4. One restart of `imad-automation`, one cert attempt.
5. Report the raw Traefik log from that attempt — success or failure —
   plus confirm via `docker exec traefik grep -o "api.imadconsulting.co.uk"
   /letsencrypt/acme.json` that the cert now actually exists (HO-031
   correctly checked this was empty before; check it again after).

### 4b. Different accounts

1. Create a new API token in whichever account holds
   `imadconsulting.co.uk`, scoped to that zone only
   (`Zone:DNS:Edit` + `Zone:Zone:Read`).
2. Add a second certresolver in `traefik.yml` using that token — OpenCode,
   prepare this change but do not apply it until Malachy confirms the
   branch.
3. Update the `imad-automation` router's Traefik label to reference the
   new certresolver explicitly (`tls.certresolver=<new-resolver-name>`)
   rather than the default one used by `zubbystudio.site` services.
4. Same wait-for-window, one-attempt, raw-log-report discipline as 4a
   steps 3–5.
5. Do not pursue the HTTP-01 fallback unless this path is also somehow
   blocked — same reasoning as HO-030 §3: it trades one dependency for two
   unverified ones (port 80 reachability, proxy status).

## 5. Clarification accepted from HO-031 §5 item 3

Good catch, worth locking in explicitly: HO-026's Task 3 verification
(button-swap, scroll target, `source_campaign` field) tested the
*rendered page*, which never required the API to be reachable, so that
part was genuinely proven. What's still unproven is the dual-submit
*backend* leg through a real browser — that's the piece HO-025 §3 actually
asked for, and it remains blocked until the cert issue is resolved (this
handover) and the Telegram bot token is real (HO-026 §6.2). Both
conditions need to be true before that specific test can run for real.

## 6. Status

Waiting on: (1) Malachy's 30-second dashboard check, (2) password rotation
— no reason for this one to wait on anything. Once §2 is answered, execute
the matching branch in §4 and report back per the raw-evidence standard
used throughout this thread.
