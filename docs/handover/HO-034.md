---
type: HANDOVER
project: IMAD Consulting — Lead Capture & Content Automation
title: HO-034 — Cert issued via HTTP-01, password rotated, failure-path drill passed
date: 2026-09-21
from: OpenCode / DeepSeek-flash (implementation)
to: Claude (architecture/backend) — review
status: UNBLOCKED — production API live over valid TLS; Telegram token still pending
priority: NORMAL
---

## 1. §4 — Password rotation: CONFIRMED DONE

`imad_user`'s password has been rotated. New value never written to any
handover (kept only in `.env`, which is gitignored).

Evidence the new credential actually works (not just that `ALTER USER` ran):

```
$ docker exec imad-automation python -c "... POST /api/leads ..."
insert status: 200 {"status":"ok","lead_id":2}

$ docker ps --filter name=imad-automation --format "{{.Status}}"
Up (healthy)
```

Startup log with the new credential (no `InvalidPasswordError`):

```
2026-09-20 22:18:20 INFO imad.db: db pool initialized
INFO:     Application startup complete.
```

**Also fixed while doing this:** `docker-compose.yml` had the *old* password
hardcoded (my own error from the initial deploy, and a committed-secret
violation). It now reads `${IMAD_DATABASE_URL}` from `.env` — the old value
in git history is rotated and dead. Commit `f7c5594`.

---

## 2. §2 Option A — Both verification checks pass

### Port 80 externally reachable

Checked from four external nodes via check-host.net (not the VPS):

```
fr2 (France): 301 Moved Permanently  from 185.216.177.250
ir1 (Iran):   301 Moved Permanently  from 185.216.177.250
ir5 (Iran):   301 Moved Permanently  from 185.216.177.250
ru1 (Russia): 301 Moved Permanently  from 185.216.177.250
```

All four reached Traefik on port 80 and got a redirect — port 80 is open
externally.

### Not Cloudflare-proxied (DNS-only / grey cloud)

```
$ dig +short api.imadconsulting.co.uk
185.216.177.250          # = the VPS IP, not a Cloudflare edge IP
```

A proxied (orange-cloud) record would resolve to Cloudflare's own IPs
(104.x / 172.x). It resolves straight to the origin, so the record is
DNS-only — exactly what HTTP-01 needs.

---

## 3. Option A implemented

### `traefik.yml` (outside this repo: `/home/zubbyik/openagile/configs/traefik/traefik.yml`)

Added a second resolver alongside the existing DNS-01 one — the DNS-01
resolver and the `zubbystudio.site` services are completely untouched:

```yaml
certificatesResolvers:
  cloudflare:            # existing DNS-01 resolver — unchanged
    acme:
      ...
      dnsChallenge:
        provider: cloudflare

  letsencrypt-http:      # NEW — HTTP-01, no Cloudflare credentials
    acme:
      email: zubbyik@gmail.com
      storage: /letsencrypt/acme-http.json
      httpChallenge:
        entryPoint: web
```

Backup: `traefik.yml.bak-20260921-002057`.

### `docker-compose.yml` — one label changed (commit `f7c5594`)

```
- "traefik.http.routers.imad-automation.tls.certresolver=cloudflare"
+ "traefik.http.routers.imad-automation.tls.certresolver=letsencrypt-http"
```

Traefik restarted (static config) and `imad-automation` recreated.

---

## 4. Cert issued — ONE clean attempt, success

Rate-limit window confirmed expired before attempting (HO-031 §3 recorded
`retry after 2026-09-20 22:20:38 UTC`; attempt made well after that).

Raw Traefik log from the attempt:

```
level=info msg="Server responded with a certificate." lib=lego domains=api.imadconsulting.co.uk
```

Cert exists in the new, separate storage file:

```
$ docker exec traefik sh -c 'grep -o "api.imadconsulting.co.uk" /letsencrypt/acme-http.json | head -1'
api.imadconsulting.co.uk

$ echo | openssl s_client -connect api.imadconsulting.co.uk:443 -servername api.imadconsulting.co.uk
subject=CN = api.imadconsulting.co.uk
issuer=C = US, O = Let's Encrypt, CN = YR2
notBefore=Sep 21 11:32:07 2026 GMT
notAfter=Dec 20 11:32:06 2026 GMT
```

**Task 1 acceptance criterion now met:**

```
$ curl -s https://api.imadconsulting.co.uk/healthz
{"status":"ok","time":"2026-09-21T12:30:50.961952+00:00"}
```

Click-tracking also verified:

```
$ curl -s -o /dev/null -w "%{http_code} %{redirect_url}" \
    "https://api.imadconsulting.co.uk/r/status?campaign=ep001"
307 https://imadconsulting.co.uk/?utm_source=status&utm_campaign=ep001

$ psql ... SELECT source, campaign FROM clicks ...
 status | ep001
```

---

## 5. HO-025 §3 failure-path drill — all three passed

Tested through the real browser on staging, with `?utm_source=status&utm_campaign=ep001`.

### Test 1 — backend down → email must still deliver ✅

- `docker stop imad-automation`, then submitted via the browser.
- Network tab: `POST api.imadconsulting.co.uk/api/leads` → `FAILED (net::ERR_FAILED)`;
  `POST admin-ajax.php` → `200`.
- Visitor saw **"Thanks! I'll get back to you soon."**, form reset.

### Test 2 — WordPress AJAX down → backend must still receive the lead ✅

- Restored the backend; blocked `**/wp-admin/admin-ajax.php` at the browser
  route level (aborted), then submitted.
- Visitor still saw success.
- Lead **was created in the database**, with correct attribution:

```
 id |       name        |          contact          | source_campaign
----+-------------------+---------------------------+-----------------
  4 | FailTest AJAXDown | failtest-ajax@example.com | ep001
```

### Test 3 — both healthy → both deliver ✅

- Lead #3, same path, with both requests returning `200`:

```
31. [POST] https://api.imadconsulting.co.uk/api/leads        => [200]
32. [POST] https://imadconsult.zubbystudio.site/wp-admin/admin-ajax.php => [200]

 id |     name     |         contact          | source_campaign | status
----+--------------+--------------------------+-----------------+--------
  3 | Browser Test | browser-test@example.com | ep001           | new
```

**Task 3 acceptance also met end-to-end:** the `source_campaign = 'ep001'`
column value is confirmed in the `leads` table from a real browser
submission via the swapped-button path.

### One honest limitation on the drill

Test 2 proves the **backend received and stored** the lead when the email
path failed. It does **not** prove a Telegram message arrived, because the
Telegram bot token is still the placeholder `TODO_CREATE_BOT_VIA_BOTFATHER`.
Failed notifications are correctly captured rather than lost:

```
$ psql ... SELECT id, kind, error FROM dead_letters ...
 1 | new_lead | RetryError[... HTTPStatusError]
 2 | new_lead | RetryError[... HTTPStatusError]
 3 | new_lead | RetryError[... HTTPStatusError]
```

So the *resilience* claim is fully proven (neither channel depends on the
other); the *Telegram delivery* claim is unproven until the token is real.

---

## 6. Open items

1. **Telegram bot token** — still a placeholder. Create via @BotFather, set
   `IMAD_TELEGRAM_BOT_TOKEN` + `IMAD_TELEGRAM_CHAT_ID` in `.env`, recreate
   the container. Then re-run Test 3 and confirm a Telegram message arrives.
2. **Dead-letter recovery** — the 3 rows above are harmless test artefacts;
   clear them once the token is live, or leave them as the recovery queue.
3. **Pre-existing, unrelated:** Traefik logs show cert-renewal failures for
   several `zubbystudio.shop` hosts (`cloudflare: failed to find zone shop.`)
   — the CF token can't see the `.shop` zone either. Not caused by this
   change, but worth flagging since it means those services' certs are
   failing renewal too.

---

## 7. What was NOT done

- No change to the DNS-01 `cloudflare` resolver or any `zubbystudio.site`
  service.
- No HTTP-01 fallback was needed beyond Option A (which *is* HTTP-01, as
  HO-033 recommended) — Option B (second Traefik instance) not pursued.
- `contact.js` and the backend app are unchanged this round.
