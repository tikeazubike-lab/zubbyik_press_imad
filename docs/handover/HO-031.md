---
type: HANDOVER
project: IMAD Consulting — Lead Capture & Content Automation
title: HO-031 — HO-030 §2 checks pass, §3 blocked on dashboard, no retry attempted
date: 2026-09-20
from: OpenCode / DeepSeek-flash (implementation)
to: Claude (architecture/backend) — decision needed
status: BLOCKED — awaiting Cloudflare account-boundary check
priority: NORMAL
---

## 1. §2 — File checks (raw output, all three)

```
$ grep -n "IMAD_API_URL\|API_URL" ~/wordpress_project/malachy-portfolio/assets/js/contact.js
96:  var IMAD_API_URL = 'https://api.imadconsulting.co.uk/api/leads';
151:      fetch(IMAD_API_URL, {

$ grep -n "max_length" ~/wordpress_project/imad-automation/app/models.py
5:    name: str = Field(min_length=1, max_length=200)
6:    contact: str = Field(min_length=3, max_length=200)  # email or whatsapp number
7:    problem_text: str = Field(min_length=5, max_length=4000)
8:    source_campaign: str | None = Field(default=None, max_length=100)
9:    idempotency_key: str = Field(min_length=8, max_length=128)
17:    website: str = Field(default="", max_length=500, description="honeypot — must stay empty")
21:    topic: str = Field(min_length=3, max_length=300)

$ grep -n "ALLOWED_ORIGIN" ~/wordpress_project/imad-automation/app/config.py
45:    ALLOWED_ORIGINS: list[str] = [
47:            "IMAD_ALLOWED_ORIGINS", "https://imadconsulting.co.uk"
```

**Verdict: HO-029 is fully applied.** No contradiction remains:

- `contact.js:96` → `api.imadconsulting.co.uk` ✅
- `models.py:17` → `max_length=500` (not `0`) ✅
- `config.py:45` → `ALLOWED_ORIGINS` (plural) ✅

**Correction to my earlier statement:** I said "the `_3.zip` matches what's deployed" — that was imprecise. The zips are all named `imad-automation.zip` upstream and overwritten; the `_3` suffix was from the local copy filename `imad-automation_3.zip` I extracted on disk. The correct claim is: **the code currently deployed matches `imad-automation_3.zip`, and that code is already the `api.imadconsulting.co.uk` revision** — i.e. HO-029's reversion is present in what's deployed, regardless of the zip's local suffix. Verified by the three greps above, not by memory.

---

## 2. §3 — Cloudflare account boundary: cannot be determined from this server

HO-030 §3 asks for the dashboard check first. That requires Cloudflare UI access I don't have. Here is everything the API can tell us with the token Traefik holds:

### Token scope evidence

```
$ curl .../zones?per_page=50
['zubbystudio.site']

$ curl .../zones?name=imadconsulting.co.uk
[]                    (empty — zone not visible to this token)

$ curl .../accounts
[]                    (empty — token is zone-scoped, not account-wide)

$ curl .../user/tokens/verify
token id: ba16bfcbb462ed21305946d28f28482b | status: active
```

### Nameserver evidence

```
$ dig NS imadconsulting.co.uk +short
robert.ns.cloudflare.com.
journey.ns.cloudflare.com.

$ dig NS zubbystudio.site +short
naomi.ns.cloudflare.com.
sid.ns.cloudflare.com.
```

### Interpretation (not a conclusion)

- Both zones are on Cloudflare.
- The token is **zone-scoped** (it cannot list accounts), so its inability to see `imadconsulting.co.uk` means that zone is either in a different account, **or** in the same account but not included in this token's Zone Resources.
- The differing nameserver pairs are suggestive but **not conclusive** — one Cloudflare account can hold zones with different assigned nameserver pairs. So this evidence cannot decide §3's branch on its own.

**This is the one thing I cannot resolve from the server.** It requires logging into the Cloudflare dashboard and checking whether both zones appear under the same account.

---

## 3. §4 — Rate limit state: window nearly expired, no retry performed

Most recent relevant Traefik line:

```
time="2026-09-20T23:04:25+01:00" level=error msg="Unable to obtain ACME certificate
for domains \"api.imadconsulting.co.uk\": ... 429 ... rateLimited :: too many failed
authorizations (5) for \"api.imadconsulting.co.uk\" in the last 1h0m0s,
retry after 2026-09-20 22:08:39 UTC ..."
```

- Retry-after: `2026-09-20 22:08:39 UTC`
- Current time at check: `2026-09-20 22:04:45 UTC` (≈4 min remaining)

**No retry was attempted.** Per HO-030 §4, a retry before the token is fixed would fail the authorization again and extend the penalty — exactly the loop that caused this. The window expiring is necessary but not sufficient; the token must be fixed first.

### Certificate state

```
$ docker exec traefik grep -o "api.imadconsulting.co.uk" /letsencrypt/acme.json
(no output)
```

No certificate for `api.imadconsulting.co.uk` exists in `acme.json` — it has never been issued.

---

## 4. Decision required

The fix cannot proceed without the §3 dashboard check. Three paths, per HO-030 §3:

1. **Same account (preferred):** add `imadconsulting.co.uk` to the existing token's Zone Resources (`Zone:DNS:Edit` + `Zone:Zone:Read`). No `traefik.yml` change. Then one clean retry after the rate-limit window.
2. **Different accounts:** second certresolver + second token scoped to the `imadconsulting.co.uk` account. Requires a `traefik.yml` edit (I can prepare this on request).
3. **HTTP-01 fallback (last resort only):** not recommended — trades one dependency for two (port 80 reachability + proxy status) per HO-030 §3.

**Blocking question for Malachy/Claude:** are `imadconsulting.co.uk` and `zubbystudio.site` in the same Cloudflare account? Until that is answered, I am deliberately not touching `traefik.yml` or retrying the cert.

---

## 5. §5 — Still outstanding (unaffected by this work)

1. **Postgres password rotation** — `imad_user` password is currently `imad_2026_secure`, written in plaintext in HO-026. Urgent.
2. **HO-025 §3 failure-path proof** — blocked in part on the Telegram bot token still being a placeholder (`TODO_CREATE_BOT_VIA_BOTFATHER` in `.env`), so Telegram notifications currently land in `dead_letters` rather than being delivered.
3. **Clarification on HO-026 §3's "browser-confirmed" test** — HO-026 claimed the hero CTA was verified in a real browser, but `api.imadconsulting.co.uk` had no reachable cert at the time. The Task 3 verification (button swap, scroll target, `source_campaign` field) was performed on the rendered page and does not depend on the API being reachable; the dual-submit *backend* leg was not exercised through a browser. This needs to be stated plainly rather than left implied.

---

## 6. Actions taken this session

- Ran the three §2 greps — all pass, HO-029 confirmed applied.
- Gathered all API-side evidence for the account-boundary question (token scope, zone visibility, nameservers).
- Checked the current rate-limit retry-after and the `acme.json` cert state.
- **Stopped.** No `traefik.yml` change, no container restart, no cert retry — all deferred until §3 is answered.
