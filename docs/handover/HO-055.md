---
type: HANDOVER
project: IMAD Consulting — Lead Capture & Content Automation
title: HO-055 — Review of HO-054: authorization question first, technical findings mostly accepted
date: 2026-09-25
from: Claude (Reviewer)
to: Malachy (one question, urgent) / Hermes (technical response)
status: PARTIAL — technical content reviewed; structural legitimacy unconfirmed
priority: URGENT (§1 only — technical review in §2+ is normal priority)
---

## 1. Malachy — I need you to confirm this directly before I treat HO-054 as settled

Same situation as HO-044, but with more at stake this time: §12 reports that
commits were already made and **pushed to `origin/main`** based on a
"go-ahead" that happened entirely outside this review thread. Real
repository state changed on the strength of an agent's own account that
you authorized it — I have no independent way to verify that from here,
and the project's own precedent (HO-044/045) was that structural or
authorization-bearing claims get confirmed by you directly, not accepted
because an agent asserts them well.

Two things, plainly:

1. **Did you commission "Hermes" as a new orchestrator role** on this
   project? The four-role model confirmed in HO-045 (MiMo Architect/Flash
   Implementer/Claude Reviewer/ChatGPT Co-reviewer) doesn't include an
   orchestrator layer above it, and §7 C4 of HO-054 itself flags that this
   session's model (`deepseek-v4.1-flash`) doesn't match any of the four
   model identities already on record for this project — including its
   own commissioning brief, which apparently names yet a *different*
   model (Kimi K2.7 Code). That's four conflicting identities across this
   project's own documents now, and Hermes correctly declined to resolve
   that itself (B-04) rather than guess — which is the right instinct, but
   it still leaves the actual answer needed.
2. **Did you personally give the go-ahead described in §12** — the one
   that resulted in four commits landing on `origin/main`? Not "does this
   look like something you'd approve," but did it actually happen, and did
   you understand at the time that it meant real git history was about to
   change on the strength of a first-run discovery report.

I want to be clear this isn't a judgment on Hermes's work — the technical
content below is genuinely strong, better-evidenced than a lot of what's
come through this thread. The concern is procedural: this project has a
standing habit, set by you, of routing structural changes through explicit
confirmation rather than agent self-assertion, and §12 is exactly the kind
of thing that habit exists to catch, whether or not anything is actually
wrong this time.

**Nothing further should proceed to production (T-01 onward) until this is
answered.**

## 2. Technical review — proceeding on the content's own merits

### 2.1 Accepted as well-evidenced

- **§2, live production defects**: a placeholder Search Console
  verification token and a stale `Portfolio · 2026` meta description are
  genuinely live right now, confirmed by a direct GET against the real
  URL with the actual rendered `<meta>` tags quoted. This is the same
  issue flagged back in the original SEO Phase 0 work — good that it's
  been re-confirmed with a fresh probe rather than assumed fixed.
- **§6.3, form field parity**: five specific, file-and-line-cited gaps
  (`source_campaign` posted but never read; `malachy_service` missing from
  the Telegram JSON payload; three settings fields with no
  `register_setting` entry, silently discarded on save; two admin pages
  sharing one option group). This is exactly the failure class this
  project has been burned by twice already (shared code validated against
  only one caller), and the level of specificity here (real file paths,
  real line numbers) is the standard this project has needed and not
  always gotten. Accepted, pending independent verification once T-03 is
  actually authorized.
- **§6.4, dead follow-up reminder**: `app/db.py:70` filtering on
  `leads.status='contacted'` when nothing ever writes that column is a
  real, specific, falsifiable claim — and if true, it means a feature the
  operator manual tells Malachy to use has never been able to fire. Worth
  independent confirmation, but this is a strong, well-formed finding.
- **§7, C1–C10**: the documentation-vs-reality conflict table is useful
  precisely because it doesn't resolve ambiguity by picking a side — C4,
  C6, C8 are correctly left as open questions rather than guessed at.

### 2.2 One correction needed before T-04 proceeds

§6.5 lists `IMAD_FORM_SECRET`'s presence in `contact.js` as a "live secret
in tracked files," and T-04 proposes rotating it. **This is based on a
misunderstanding of an already-documented, deliberate design decision.**
`IMAD_FORM_SECRET` was never meant to be secret — it's a bot-filter value,
intentionally visible in client-side JS, established back in this
project's HO-023 and explicitly re-confirmed as "intentional by design"
in HO-036's own git-history scan. Rotating it doesn't reduce any real
risk, because it was never providing the kind of protection rotation
would restore. **Point Hermes at HO-023 and HO-036 before T-04 spends
effort here** — the actual open item in that section is the
`wordpress_pass`/`root_pass` MySQL credentials, which *are* real, were
already flagged in HO-036/037, and correctly still need remediation —
they've just never been given an actual date, same gap as before. If T-04
proceeds, it should proceed on those, not on the form secret.

### 2.3 One structural note, not a new problem

§0's status line reads "AWAITING REVIEW BEFORE IMPLEMENTATION," but §12
reports commits and a push already happened before this review — under
the "discovery only, no code changed" framing that technically held (no
*application* code changed, no production deploy happened), but committing
and pushing to `origin/main` is not nothing. Going forward: if a go-ahead
happens outside this thread, say so plainly in the handover *before*
describing what was done with it, not as an addendum after the fact once
this document has already opened by promising nothing happened yet.

## 3. Status

Blocked on §1. Once Malachy confirms directly, the roadmap in HO-054 §8
can be reviewed for sequencing (it reads sensibly at a glance — Phase 1
prioritizing the live production defects and field-parity gaps ahead of
hygiene work is the right order), but I'm not signing off on sequencing a
roadmap whose commissioning authority is still an open question.
