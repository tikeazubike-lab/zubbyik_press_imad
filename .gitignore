node_modules/
test-results/
qa-screenshot.cjs
*.log
.env
.DS_Store
build/
db-data/
wp-data/
malachy-s-cinematic-canvas/
human_image.png
imadconsult.zubbystudio.site*.json
instruction_claude.md
My Blog*.png
Tests/
.env.imad

# Local-only caches and visual-test artifacts (never commit these):
# .playwright-browsers/ is ~656 MB; visual-baseline/ is regenerable output
.playwright-browsers/
.playwright-mcp/
tests/visual/visual-baseline/

# ---------------------------------------------------------------------------
# Heavy / regenerable local artifacts — keep on disk, never commit.
# Root-anchored (leading /) on purpose: theme images under
# malachy-portfolio/assets/images/ and the already-tracked
# docs/handover/*.png are NOT affected by these rules.
# ---------------------------------------------------------------------------
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.gif
/*.zip
/*.tar.gz
/*-snapshot.yml
/imadconsult*.json
__pycache__/
*.pyc

# Test output and the untracked React reference project
# (polished-portfolio was deliberately removed from tracking in 5ec4df4)
/assets/tests/
/polished-portfolio/
